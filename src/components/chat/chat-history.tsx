
// This component is no longer used, as chat history is now managed within chat-layout.tsx
// To keep the project clean, I'm providing an empty component.
// You can also delete this file.

export default function ChatHistory({ userId }: { userId: string }) {
  return null;
}
